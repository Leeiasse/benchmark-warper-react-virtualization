# Database connection (overrides root .env if set)
POSTGRES_HOST=localhost
POSTGRES_PORT=5452
POSTGRES_USER=bench
POSTGRES_PASSWORD=bench_password
POSTGRES_DB=bench_virtualization

# API server
API_PORT=3100

# CORS (comma-separated origins)
CORS_ORIGIN=http://localhost:5173,http://localhost:4173,http://localhost:3000
