# Database
POSTGRES_USER=bench
POSTGRES_PASSWORD=bench_password
POSTGRES_DB=bench_virtualization
POSTGRES_PORT=5452

# Docker: exposed ports
API_PORT=3100
APP_PORT=3000

# pgAdmin (docker compose --profile tools up)
PGADMIN_EMAIL=admin@bench.local
PGADMIN_PASSWORD=admin
